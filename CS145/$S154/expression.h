/* Various utilities for Regular Expressions including flattening the
   expression tree into levels of equal (though non-commutative, in
   general) precedence, merging character sets in union operations,
   printing trees and character sets, and performing other useful
   functions */

#ifndef _EXPRESSION_H_
#define _EXPRESSION_H_

#include "parser.h"

#endif
